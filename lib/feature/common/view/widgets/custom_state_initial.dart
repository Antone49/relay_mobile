import 'package:flutter/material.dart';

class CustomStateInitial extends StatelessWidget {
  const CustomStateInitial({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Initial state'));
  }
}